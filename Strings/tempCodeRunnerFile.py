 # for key, value in char_counter.items():
    #     current_max = max(char_counter.values())
    #     if value == current_max:
    #         char_sorted[key] = value
    #         char_counter.pop(key)
    # print(char_sorted)